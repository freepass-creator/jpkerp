/**
 * pages/total.js — 통합관리
 * 차량번호 기준 전체 데이터 조인 — 한 행에 자산+계약+수납+할부 전부
 */
export async function mount() {
  document.getElementById('totalGrid').innerHTML = '<div style="padding:48px;text-align:center;color:var(--c-text-muted)">통합관리 준비 중</div>';
}
