/**
 * pages/operation.js — 운영관리 (운영 이벤트 조회)
 */
export async function mount() {
  // TODO: 운영 이벤트 AG Grid 조회
  document.getElementById('operationGrid').innerHTML = '<div style="padding:48px;text-align:center;color:var(--c-text-muted)">운영관리 준비 중</div>';
}
